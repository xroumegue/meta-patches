# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/master/kas/schema-kas.json

header:
  version: 14

repos:
  meta-arm:
    patches:
      plain:
        repo: meta-patches
        path: |
          arm/0001-devtools-opencsd-Remove-WORKDIR-usage.patch
          arm/0002-gator-daemon-Update-working-folders.patch
